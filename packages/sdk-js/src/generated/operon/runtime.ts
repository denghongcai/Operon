// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.6
//   protoc               v3.12.4
// source: operon/runtime.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import type { CallContext, CallOptions } from "nice-grpc-common";

export const protobufPackage = "operon.runtime.v1";

export interface HealthRequest {
}

export interface GetNodeRequest {
}

export interface ListCapabilitiesRequest {
}

export interface ListJobsRequest {
}

export interface ListServicesRequest {
}

export interface ListAuditRequest {
}

export interface HealthStatus {
  ok: boolean;
  nodeId: string;
  version: string;
}

export interface NodeInfo {
  id: string;
  hostname: string;
  os: string;
  arch: string;
}

export interface Capability {
  id: string;
  kind: string;
  nodeId: string;
  name: string;
  permissions: string[];
  description: string;
}

export interface CapabilityList {
  capabilities: Capability[];
}

export interface FsPathRequest {
  path: string;
}

export interface FsStat {
  path: string;
  isFile: boolean;
  isDir: boolean;
  size: string;
}

export interface FsEntry {
  name: string;
  path: string;
  isFile: boolean;
  isDir: boolean;
  size: string;
}

export interface FsList {
  path: string;
  entries: FsEntry[];
}

export interface FileChunk {
  data: Uint8Array;
}

export interface WriteFileRequest {
  path: string;
  data: Uint8Array;
}

export interface FsWrite {
  path: string;
  bytesWritten: string;
}

export interface JobRunRequest {
  command: string;
  cwd: string;
  timeoutSecs: string;
  secrets: string[];
  hasTimeoutSecs: boolean;
}

export interface JobIdRequest {
  jobId: string;
}

export interface JobCancelRequest {
  jobId: string;
}

export interface JobLog {
  stream: string;
  data: string;
}

export interface JobRecord {
  id: string;
  nodeId: string;
  command: string;
  cwd: string;
  status: string;
  exitCode: number;
  logs: JobLog[];
  hasExitCode: boolean;
}

export interface JobList {
  jobs: JobRecord[];
}

export interface JobStdinRequest {
  jobId: string;
  data: Uint8Array;
}

export interface JobStdin {
  jobId: string;
  bytesWritten: string;
}

export interface JobStdinClose {
  jobId: string;
  closed: boolean;
}

export interface ServiceIdRequest {
  serviceId: string;
}

export interface ServiceDefinition {
  id: string;
  name: string;
  host: string;
  port: number;
  protocol: string;
  description: string;
}

export interface ServiceList {
  services: ServiceDefinition[];
}

export interface ServiceCheck {
  id: string;
  ok: boolean;
  latencyMs: string;
  reason: string;
  hasReason: boolean;
}

export interface AuditEvent {
  subject: string;
  timestampMs: string;
  nodeId: string;
  capability: string;
  action: string;
  resource: string;
  allowed: boolean;
  reason: string;
  runId: string;
  stepId: string;
  hasRunId: boolean;
  hasStepId: boolean;
}

export interface AuditLog {
  events: AuditEvent[];
}

function createBaseHealthRequest(): HealthRequest {
  return {};
}

export const HealthRequest: MessageFns<HealthRequest> = {
  encode(_: HealthRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): HealthRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHealthRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): HealthRequest {
    return {};
  },

  toJSON(_: HealthRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<HealthRequest>): HealthRequest {
    return HealthRequest.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<HealthRequest>): HealthRequest {
    const message = createBaseHealthRequest();
    return message;
  },
};

function createBaseGetNodeRequest(): GetNodeRequest {
  return {};
}

export const GetNodeRequest: MessageFns<GetNodeRequest> = {
  encode(_: GetNodeRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetNodeRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetNodeRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): GetNodeRequest {
    return {};
  },

  toJSON(_: GetNodeRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<GetNodeRequest>): GetNodeRequest {
    return GetNodeRequest.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<GetNodeRequest>): GetNodeRequest {
    const message = createBaseGetNodeRequest();
    return message;
  },
};

function createBaseListCapabilitiesRequest(): ListCapabilitiesRequest {
  return {};
}

export const ListCapabilitiesRequest: MessageFns<ListCapabilitiesRequest> = {
  encode(_: ListCapabilitiesRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListCapabilitiesRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListCapabilitiesRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): ListCapabilitiesRequest {
    return {};
  },

  toJSON(_: ListCapabilitiesRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<ListCapabilitiesRequest>): ListCapabilitiesRequest {
    return ListCapabilitiesRequest.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<ListCapabilitiesRequest>): ListCapabilitiesRequest {
    const message = createBaseListCapabilitiesRequest();
    return message;
  },
};

function createBaseListJobsRequest(): ListJobsRequest {
  return {};
}

export const ListJobsRequest: MessageFns<ListJobsRequest> = {
  encode(_: ListJobsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListJobsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListJobsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): ListJobsRequest {
    return {};
  },

  toJSON(_: ListJobsRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<ListJobsRequest>): ListJobsRequest {
    return ListJobsRequest.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<ListJobsRequest>): ListJobsRequest {
    const message = createBaseListJobsRequest();
    return message;
  },
};

function createBaseListServicesRequest(): ListServicesRequest {
  return {};
}

export const ListServicesRequest: MessageFns<ListServicesRequest> = {
  encode(_: ListServicesRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListServicesRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListServicesRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): ListServicesRequest {
    return {};
  },

  toJSON(_: ListServicesRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<ListServicesRequest>): ListServicesRequest {
    return ListServicesRequest.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<ListServicesRequest>): ListServicesRequest {
    const message = createBaseListServicesRequest();
    return message;
  },
};

function createBaseListAuditRequest(): ListAuditRequest {
  return {};
}

export const ListAuditRequest: MessageFns<ListAuditRequest> = {
  encode(_: ListAuditRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListAuditRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListAuditRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): ListAuditRequest {
    return {};
  },

  toJSON(_: ListAuditRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<ListAuditRequest>): ListAuditRequest {
    return ListAuditRequest.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<ListAuditRequest>): ListAuditRequest {
    const message = createBaseListAuditRequest();
    return message;
  },
};

function createBaseHealthStatus(): HealthStatus {
  return { ok: false, nodeId: "", version: "" };
}

export const HealthStatus: MessageFns<HealthStatus> = {
  encode(message: HealthStatus, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.ok !== false) {
      writer.uint32(8).bool(message.ok);
    }
    if (message.nodeId !== "") {
      writer.uint32(18).string(message.nodeId);
    }
    if (message.version !== "") {
      writer.uint32(26).string(message.version);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): HealthStatus {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHealthStatus();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.ok = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.nodeId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.version = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): HealthStatus {
    return {
      ok: isSet(object.ok) ? globalThis.Boolean(object.ok) : false,
      nodeId: isSet(object.nodeId)
        ? globalThis.String(object.nodeId)
        : isSet(object.node_id)
        ? globalThis.String(object.node_id)
        : "",
      version: isSet(object.version) ? globalThis.String(object.version) : "",
    };
  },

  toJSON(message: HealthStatus): unknown {
    const obj: any = {};
    if (message.ok !== false) {
      obj.ok = message.ok;
    }
    if (message.nodeId !== "") {
      obj.nodeId = message.nodeId;
    }
    if (message.version !== "") {
      obj.version = message.version;
    }
    return obj;
  },

  create(base?: DeepPartial<HealthStatus>): HealthStatus {
    return HealthStatus.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<HealthStatus>): HealthStatus {
    const message = createBaseHealthStatus();
    message.ok = object.ok ?? false;
    message.nodeId = object.nodeId ?? "";
    message.version = object.version ?? "";
    return message;
  },
};

function createBaseNodeInfo(): NodeInfo {
  return { id: "", hostname: "", os: "", arch: "" };
}

export const NodeInfo: MessageFns<NodeInfo> = {
  encode(message: NodeInfo, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.hostname !== "") {
      writer.uint32(18).string(message.hostname);
    }
    if (message.os !== "") {
      writer.uint32(26).string(message.os);
    }
    if (message.arch !== "") {
      writer.uint32(34).string(message.arch);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): NodeInfo {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseNodeInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.hostname = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.os = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.arch = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): NodeInfo {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      hostname: isSet(object.hostname) ? globalThis.String(object.hostname) : "",
      os: isSet(object.os) ? globalThis.String(object.os) : "",
      arch: isSet(object.arch) ? globalThis.String(object.arch) : "",
    };
  },

  toJSON(message: NodeInfo): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.hostname !== "") {
      obj.hostname = message.hostname;
    }
    if (message.os !== "") {
      obj.os = message.os;
    }
    if (message.arch !== "") {
      obj.arch = message.arch;
    }
    return obj;
  },

  create(base?: DeepPartial<NodeInfo>): NodeInfo {
    return NodeInfo.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<NodeInfo>): NodeInfo {
    const message = createBaseNodeInfo();
    message.id = object.id ?? "";
    message.hostname = object.hostname ?? "";
    message.os = object.os ?? "";
    message.arch = object.arch ?? "";
    return message;
  },
};

function createBaseCapability(): Capability {
  return { id: "", kind: "", nodeId: "", name: "", permissions: [], description: "" };
}

export const Capability: MessageFns<Capability> = {
  encode(message: Capability, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.kind !== "") {
      writer.uint32(18).string(message.kind);
    }
    if (message.nodeId !== "") {
      writer.uint32(26).string(message.nodeId);
    }
    if (message.name !== "") {
      writer.uint32(34).string(message.name);
    }
    for (const v of message.permissions) {
      writer.uint32(42).string(v!);
    }
    if (message.description !== "") {
      writer.uint32(50).string(message.description);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Capability {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCapability();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.kind = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.nodeId = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.permissions.push(reader.string());
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.description = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Capability {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      kind: isSet(object.kind) ? globalThis.String(object.kind) : "",
      nodeId: isSet(object.nodeId)
        ? globalThis.String(object.nodeId)
        : isSet(object.node_id)
        ? globalThis.String(object.node_id)
        : "",
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      permissions: globalThis.Array.isArray(object?.permissions)
        ? object.permissions.map((e: any) => globalThis.String(e))
        : [],
      description: isSet(object.description) ? globalThis.String(object.description) : "",
    };
  },

  toJSON(message: Capability): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.kind !== "") {
      obj.kind = message.kind;
    }
    if (message.nodeId !== "") {
      obj.nodeId = message.nodeId;
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.permissions?.length) {
      obj.permissions = message.permissions;
    }
    if (message.description !== "") {
      obj.description = message.description;
    }
    return obj;
  },

  create(base?: DeepPartial<Capability>): Capability {
    return Capability.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Capability>): Capability {
    const message = createBaseCapability();
    message.id = object.id ?? "";
    message.kind = object.kind ?? "";
    message.nodeId = object.nodeId ?? "";
    message.name = object.name ?? "";
    message.permissions = object.permissions?.map((e) => e) || [];
    message.description = object.description ?? "";
    return message;
  },
};

function createBaseCapabilityList(): CapabilityList {
  return { capabilities: [] };
}

export const CapabilityList: MessageFns<CapabilityList> = {
  encode(message: CapabilityList, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.capabilities) {
      Capability.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CapabilityList {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCapabilityList();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.capabilities.push(Capability.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CapabilityList {
    return {
      capabilities: globalThis.Array.isArray(object?.capabilities)
        ? object.capabilities.map((e: any) => Capability.fromJSON(e))
        : [],
    };
  },

  toJSON(message: CapabilityList): unknown {
    const obj: any = {};
    if (message.capabilities?.length) {
      obj.capabilities = message.capabilities.map((e) => Capability.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<CapabilityList>): CapabilityList {
    return CapabilityList.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<CapabilityList>): CapabilityList {
    const message = createBaseCapabilityList();
    message.capabilities = object.capabilities?.map((e) => Capability.fromPartial(e)) || [];
    return message;
  },
};

function createBaseFsPathRequest(): FsPathRequest {
  return { path: "" };
}

export const FsPathRequest: MessageFns<FsPathRequest> = {
  encode(message: FsPathRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.path !== "") {
      writer.uint32(10).string(message.path);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FsPathRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFsPathRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.path = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FsPathRequest {
    return { path: isSet(object.path) ? globalThis.String(object.path) : "" };
  },

  toJSON(message: FsPathRequest): unknown {
    const obj: any = {};
    if (message.path !== "") {
      obj.path = message.path;
    }
    return obj;
  },

  create(base?: DeepPartial<FsPathRequest>): FsPathRequest {
    return FsPathRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<FsPathRequest>): FsPathRequest {
    const message = createBaseFsPathRequest();
    message.path = object.path ?? "";
    return message;
  },
};

function createBaseFsStat(): FsStat {
  return { path: "", isFile: false, isDir: false, size: "0" };
}

export const FsStat: MessageFns<FsStat> = {
  encode(message: FsStat, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.path !== "") {
      writer.uint32(10).string(message.path);
    }
    if (message.isFile !== false) {
      writer.uint32(16).bool(message.isFile);
    }
    if (message.isDir !== false) {
      writer.uint32(24).bool(message.isDir);
    }
    if (message.size !== "0") {
      writer.uint32(32).uint64(message.size);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FsStat {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFsStat();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.path = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.isFile = reader.bool();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.isDir = reader.bool();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.size = reader.uint64().toString();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FsStat {
    return {
      path: isSet(object.path) ? globalThis.String(object.path) : "",
      isFile: isSet(object.isFile)
        ? globalThis.Boolean(object.isFile)
        : isSet(object.is_file)
        ? globalThis.Boolean(object.is_file)
        : false,
      isDir: isSet(object.isDir)
        ? globalThis.Boolean(object.isDir)
        : isSet(object.is_dir)
        ? globalThis.Boolean(object.is_dir)
        : false,
      size: isSet(object.size) ? globalThis.String(object.size) : "0",
    };
  },

  toJSON(message: FsStat): unknown {
    const obj: any = {};
    if (message.path !== "") {
      obj.path = message.path;
    }
    if (message.isFile !== false) {
      obj.isFile = message.isFile;
    }
    if (message.isDir !== false) {
      obj.isDir = message.isDir;
    }
    if (message.size !== "0") {
      obj.size = message.size;
    }
    return obj;
  },

  create(base?: DeepPartial<FsStat>): FsStat {
    return FsStat.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<FsStat>): FsStat {
    const message = createBaseFsStat();
    message.path = object.path ?? "";
    message.isFile = object.isFile ?? false;
    message.isDir = object.isDir ?? false;
    message.size = object.size ?? "0";
    return message;
  },
};

function createBaseFsEntry(): FsEntry {
  return { name: "", path: "", isFile: false, isDir: false, size: "0" };
}

export const FsEntry: MessageFns<FsEntry> = {
  encode(message: FsEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.path !== "") {
      writer.uint32(18).string(message.path);
    }
    if (message.isFile !== false) {
      writer.uint32(24).bool(message.isFile);
    }
    if (message.isDir !== false) {
      writer.uint32(32).bool(message.isDir);
    }
    if (message.size !== "0") {
      writer.uint32(40).uint64(message.size);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FsEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFsEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.path = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.isFile = reader.bool();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.isDir = reader.bool();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.size = reader.uint64().toString();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FsEntry {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      path: isSet(object.path) ? globalThis.String(object.path) : "",
      isFile: isSet(object.isFile)
        ? globalThis.Boolean(object.isFile)
        : isSet(object.is_file)
        ? globalThis.Boolean(object.is_file)
        : false,
      isDir: isSet(object.isDir)
        ? globalThis.Boolean(object.isDir)
        : isSet(object.is_dir)
        ? globalThis.Boolean(object.is_dir)
        : false,
      size: isSet(object.size) ? globalThis.String(object.size) : "0",
    };
  },

  toJSON(message: FsEntry): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.path !== "") {
      obj.path = message.path;
    }
    if (message.isFile !== false) {
      obj.isFile = message.isFile;
    }
    if (message.isDir !== false) {
      obj.isDir = message.isDir;
    }
    if (message.size !== "0") {
      obj.size = message.size;
    }
    return obj;
  },

  create(base?: DeepPartial<FsEntry>): FsEntry {
    return FsEntry.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<FsEntry>): FsEntry {
    const message = createBaseFsEntry();
    message.name = object.name ?? "";
    message.path = object.path ?? "";
    message.isFile = object.isFile ?? false;
    message.isDir = object.isDir ?? false;
    message.size = object.size ?? "0";
    return message;
  },
};

function createBaseFsList(): FsList {
  return { path: "", entries: [] };
}

export const FsList: MessageFns<FsList> = {
  encode(message: FsList, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.path !== "") {
      writer.uint32(10).string(message.path);
    }
    for (const v of message.entries) {
      FsEntry.encode(v!, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FsList {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFsList();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.path = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.entries.push(FsEntry.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FsList {
    return {
      path: isSet(object.path) ? globalThis.String(object.path) : "",
      entries: globalThis.Array.isArray(object?.entries) ? object.entries.map((e: any) => FsEntry.fromJSON(e)) : [],
    };
  },

  toJSON(message: FsList): unknown {
    const obj: any = {};
    if (message.path !== "") {
      obj.path = message.path;
    }
    if (message.entries?.length) {
      obj.entries = message.entries.map((e) => FsEntry.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<FsList>): FsList {
    return FsList.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<FsList>): FsList {
    const message = createBaseFsList();
    message.path = object.path ?? "";
    message.entries = object.entries?.map((e) => FsEntry.fromPartial(e)) || [];
    return message;
  },
};

function createBaseFileChunk(): FileChunk {
  return { data: new Uint8Array(0) };
}

export const FileChunk: MessageFns<FileChunk> = {
  encode(message: FileChunk, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.data.length !== 0) {
      writer.uint32(10).bytes(message.data);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FileChunk {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFileChunk();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.data = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FileChunk {
    return { data: isSet(object.data) ? bytesFromBase64(object.data) : new Uint8Array(0) };
  },

  toJSON(message: FileChunk): unknown {
    const obj: any = {};
    if (message.data.length !== 0) {
      obj.data = base64FromBytes(message.data);
    }
    return obj;
  },

  create(base?: DeepPartial<FileChunk>): FileChunk {
    return FileChunk.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<FileChunk>): FileChunk {
    const message = createBaseFileChunk();
    message.data = object.data ?? new Uint8Array(0);
    return message;
  },
};

function createBaseWriteFileRequest(): WriteFileRequest {
  return { path: "", data: new Uint8Array(0) };
}

export const WriteFileRequest: MessageFns<WriteFileRequest> = {
  encode(message: WriteFileRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.path !== "") {
      writer.uint32(10).string(message.path);
    }
    if (message.data.length !== 0) {
      writer.uint32(18).bytes(message.data);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): WriteFileRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWriteFileRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.path = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.data = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): WriteFileRequest {
    return {
      path: isSet(object.path) ? globalThis.String(object.path) : "",
      data: isSet(object.data) ? bytesFromBase64(object.data) : new Uint8Array(0),
    };
  },

  toJSON(message: WriteFileRequest): unknown {
    const obj: any = {};
    if (message.path !== "") {
      obj.path = message.path;
    }
    if (message.data.length !== 0) {
      obj.data = base64FromBytes(message.data);
    }
    return obj;
  },

  create(base?: DeepPartial<WriteFileRequest>): WriteFileRequest {
    return WriteFileRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<WriteFileRequest>): WriteFileRequest {
    const message = createBaseWriteFileRequest();
    message.path = object.path ?? "";
    message.data = object.data ?? new Uint8Array(0);
    return message;
  },
};

function createBaseFsWrite(): FsWrite {
  return { path: "", bytesWritten: "0" };
}

export const FsWrite: MessageFns<FsWrite> = {
  encode(message: FsWrite, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.path !== "") {
      writer.uint32(10).string(message.path);
    }
    if (message.bytesWritten !== "0") {
      writer.uint32(16).uint64(message.bytesWritten);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FsWrite {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFsWrite();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.path = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.bytesWritten = reader.uint64().toString();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FsWrite {
    return {
      path: isSet(object.path) ? globalThis.String(object.path) : "",
      bytesWritten: isSet(object.bytesWritten)
        ? globalThis.String(object.bytesWritten)
        : isSet(object.bytes_written)
        ? globalThis.String(object.bytes_written)
        : "0",
    };
  },

  toJSON(message: FsWrite): unknown {
    const obj: any = {};
    if (message.path !== "") {
      obj.path = message.path;
    }
    if (message.bytesWritten !== "0") {
      obj.bytesWritten = message.bytesWritten;
    }
    return obj;
  },

  create(base?: DeepPartial<FsWrite>): FsWrite {
    return FsWrite.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<FsWrite>): FsWrite {
    const message = createBaseFsWrite();
    message.path = object.path ?? "";
    message.bytesWritten = object.bytesWritten ?? "0";
    return message;
  },
};

function createBaseJobRunRequest(): JobRunRequest {
  return { command: "", cwd: "", timeoutSecs: "0", secrets: [], hasTimeoutSecs: false };
}

export const JobRunRequest: MessageFns<JobRunRequest> = {
  encode(message: JobRunRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.command !== "") {
      writer.uint32(10).string(message.command);
    }
    if (message.cwd !== "") {
      writer.uint32(18).string(message.cwd);
    }
    if (message.timeoutSecs !== "0") {
      writer.uint32(24).uint64(message.timeoutSecs);
    }
    for (const v of message.secrets) {
      writer.uint32(34).string(v!);
    }
    if (message.hasTimeoutSecs !== false) {
      writer.uint32(40).bool(message.hasTimeoutSecs);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): JobRunRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseJobRunRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.command = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.cwd = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.timeoutSecs = reader.uint64().toString();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.secrets.push(reader.string());
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.hasTimeoutSecs = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): JobRunRequest {
    return {
      command: isSet(object.command) ? globalThis.String(object.command) : "",
      cwd: isSet(object.cwd) ? globalThis.String(object.cwd) : "",
      timeoutSecs: isSet(object.timeoutSecs)
        ? globalThis.String(object.timeoutSecs)
        : isSet(object.timeout_secs)
        ? globalThis.String(object.timeout_secs)
        : "0",
      secrets: globalThis.Array.isArray(object?.secrets) ? object.secrets.map((e: any) => globalThis.String(e)) : [],
      hasTimeoutSecs: isSet(object.hasTimeoutSecs)
        ? globalThis.Boolean(object.hasTimeoutSecs)
        : isSet(object.has_timeout_secs)
        ? globalThis.Boolean(object.has_timeout_secs)
        : false,
    };
  },

  toJSON(message: JobRunRequest): unknown {
    const obj: any = {};
    if (message.command !== "") {
      obj.command = message.command;
    }
    if (message.cwd !== "") {
      obj.cwd = message.cwd;
    }
    if (message.timeoutSecs !== "0") {
      obj.timeoutSecs = message.timeoutSecs;
    }
    if (message.secrets?.length) {
      obj.secrets = message.secrets;
    }
    if (message.hasTimeoutSecs !== false) {
      obj.hasTimeoutSecs = message.hasTimeoutSecs;
    }
    return obj;
  },

  create(base?: DeepPartial<JobRunRequest>): JobRunRequest {
    return JobRunRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<JobRunRequest>): JobRunRequest {
    const message = createBaseJobRunRequest();
    message.command = object.command ?? "";
    message.cwd = object.cwd ?? "";
    message.timeoutSecs = object.timeoutSecs ?? "0";
    message.secrets = object.secrets?.map((e) => e) || [];
    message.hasTimeoutSecs = object.hasTimeoutSecs ?? false;
    return message;
  },
};

function createBaseJobIdRequest(): JobIdRequest {
  return { jobId: "" };
}

export const JobIdRequest: MessageFns<JobIdRequest> = {
  encode(message: JobIdRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.jobId !== "") {
      writer.uint32(10).string(message.jobId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): JobIdRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseJobIdRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.jobId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): JobIdRequest {
    return {
      jobId: isSet(object.jobId)
        ? globalThis.String(object.jobId)
        : isSet(object.job_id)
        ? globalThis.String(object.job_id)
        : "",
    };
  },

  toJSON(message: JobIdRequest): unknown {
    const obj: any = {};
    if (message.jobId !== "") {
      obj.jobId = message.jobId;
    }
    return obj;
  },

  create(base?: DeepPartial<JobIdRequest>): JobIdRequest {
    return JobIdRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<JobIdRequest>): JobIdRequest {
    const message = createBaseJobIdRequest();
    message.jobId = object.jobId ?? "";
    return message;
  },
};

function createBaseJobCancelRequest(): JobCancelRequest {
  return { jobId: "" };
}

export const JobCancelRequest: MessageFns<JobCancelRequest> = {
  encode(message: JobCancelRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.jobId !== "") {
      writer.uint32(10).string(message.jobId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): JobCancelRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseJobCancelRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.jobId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): JobCancelRequest {
    return {
      jobId: isSet(object.jobId)
        ? globalThis.String(object.jobId)
        : isSet(object.job_id)
        ? globalThis.String(object.job_id)
        : "",
    };
  },

  toJSON(message: JobCancelRequest): unknown {
    const obj: any = {};
    if (message.jobId !== "") {
      obj.jobId = message.jobId;
    }
    return obj;
  },

  create(base?: DeepPartial<JobCancelRequest>): JobCancelRequest {
    return JobCancelRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<JobCancelRequest>): JobCancelRequest {
    const message = createBaseJobCancelRequest();
    message.jobId = object.jobId ?? "";
    return message;
  },
};

function createBaseJobLog(): JobLog {
  return { stream: "", data: "" };
}

export const JobLog: MessageFns<JobLog> = {
  encode(message: JobLog, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.stream !== "") {
      writer.uint32(10).string(message.stream);
    }
    if (message.data !== "") {
      writer.uint32(18).string(message.data);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): JobLog {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseJobLog();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.stream = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.data = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): JobLog {
    return {
      stream: isSet(object.stream) ? globalThis.String(object.stream) : "",
      data: isSet(object.data) ? globalThis.String(object.data) : "",
    };
  },

  toJSON(message: JobLog): unknown {
    const obj: any = {};
    if (message.stream !== "") {
      obj.stream = message.stream;
    }
    if (message.data !== "") {
      obj.data = message.data;
    }
    return obj;
  },

  create(base?: DeepPartial<JobLog>): JobLog {
    return JobLog.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<JobLog>): JobLog {
    const message = createBaseJobLog();
    message.stream = object.stream ?? "";
    message.data = object.data ?? "";
    return message;
  },
};

function createBaseJobRecord(): JobRecord {
  return { id: "", nodeId: "", command: "", cwd: "", status: "", exitCode: 0, logs: [], hasExitCode: false };
}

export const JobRecord: MessageFns<JobRecord> = {
  encode(message: JobRecord, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.nodeId !== "") {
      writer.uint32(18).string(message.nodeId);
    }
    if (message.command !== "") {
      writer.uint32(26).string(message.command);
    }
    if (message.cwd !== "") {
      writer.uint32(34).string(message.cwd);
    }
    if (message.status !== "") {
      writer.uint32(42).string(message.status);
    }
    if (message.exitCode !== 0) {
      writer.uint32(48).int32(message.exitCode);
    }
    for (const v of message.logs) {
      JobLog.encode(v!, writer.uint32(58).fork()).join();
    }
    if (message.hasExitCode !== false) {
      writer.uint32(64).bool(message.hasExitCode);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): JobRecord {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseJobRecord();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.nodeId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.command = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.cwd = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.status = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.exitCode = reader.int32();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.logs.push(JobLog.decode(reader, reader.uint32()));
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.hasExitCode = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): JobRecord {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      nodeId: isSet(object.nodeId)
        ? globalThis.String(object.nodeId)
        : isSet(object.node_id)
        ? globalThis.String(object.node_id)
        : "",
      command: isSet(object.command) ? globalThis.String(object.command) : "",
      cwd: isSet(object.cwd) ? globalThis.String(object.cwd) : "",
      status: isSet(object.status) ? globalThis.String(object.status) : "",
      exitCode: isSet(object.exitCode)
        ? globalThis.Number(object.exitCode)
        : isSet(object.exit_code)
        ? globalThis.Number(object.exit_code)
        : 0,
      logs: globalThis.Array.isArray(object?.logs)
        ? object.logs.map((e: any) => JobLog.fromJSON(e))
        : [],
      hasExitCode: isSet(object.hasExitCode)
        ? globalThis.Boolean(object.hasExitCode)
        : isSet(object.has_exit_code)
        ? globalThis.Boolean(object.has_exit_code)
        : false,
    };
  },

  toJSON(message: JobRecord): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.nodeId !== "") {
      obj.nodeId = message.nodeId;
    }
    if (message.command !== "") {
      obj.command = message.command;
    }
    if (message.cwd !== "") {
      obj.cwd = message.cwd;
    }
    if (message.status !== "") {
      obj.status = message.status;
    }
    if (message.exitCode !== 0) {
      obj.exitCode = Math.round(message.exitCode);
    }
    if (message.logs?.length) {
      obj.logs = message.logs.map((e) => JobLog.toJSON(e));
    }
    if (message.hasExitCode !== false) {
      obj.hasExitCode = message.hasExitCode;
    }
    return obj;
  },

  create(base?: DeepPartial<JobRecord>): JobRecord {
    return JobRecord.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<JobRecord>): JobRecord {
    const message = createBaseJobRecord();
    message.id = object.id ?? "";
    message.nodeId = object.nodeId ?? "";
    message.command = object.command ?? "";
    message.cwd = object.cwd ?? "";
    message.status = object.status ?? "";
    message.exitCode = object.exitCode ?? 0;
    message.logs = object.logs?.map((e) => JobLog.fromPartial(e)) || [];
    message.hasExitCode = object.hasExitCode ?? false;
    return message;
  },
};

function createBaseJobList(): JobList {
  return { jobs: [] };
}

export const JobList: MessageFns<JobList> = {
  encode(message: JobList, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.jobs) {
      JobRecord.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): JobList {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseJobList();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.jobs.push(JobRecord.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): JobList {
    return { jobs: globalThis.Array.isArray(object?.jobs) ? object.jobs.map((e: any) => JobRecord.fromJSON(e)) : [] };
  },

  toJSON(message: JobList): unknown {
    const obj: any = {};
    if (message.jobs?.length) {
      obj.jobs = message.jobs.map((e) => JobRecord.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<JobList>): JobList {
    return JobList.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<JobList>): JobList {
    const message = createBaseJobList();
    message.jobs = object.jobs?.map((e) => JobRecord.fromPartial(e)) || [];
    return message;
  },
};

function createBaseJobStdinRequest(): JobStdinRequest {
  return { jobId: "", data: new Uint8Array(0) };
}

export const JobStdinRequest: MessageFns<JobStdinRequest> = {
  encode(message: JobStdinRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.jobId !== "") {
      writer.uint32(10).string(message.jobId);
    }
    if (message.data.length !== 0) {
      writer.uint32(18).bytes(message.data);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): JobStdinRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseJobStdinRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.jobId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.data = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): JobStdinRequest {
    return {
      jobId: isSet(object.jobId)
        ? globalThis.String(object.jobId)
        : isSet(object.job_id)
        ? globalThis.String(object.job_id)
        : "",
      data: isSet(object.data) ? bytesFromBase64(object.data) : new Uint8Array(0),
    };
  },

  toJSON(message: JobStdinRequest): unknown {
    const obj: any = {};
    if (message.jobId !== "") {
      obj.jobId = message.jobId;
    }
    if (message.data.length !== 0) {
      obj.data = base64FromBytes(message.data);
    }
    return obj;
  },

  create(base?: DeepPartial<JobStdinRequest>): JobStdinRequest {
    return JobStdinRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<JobStdinRequest>): JobStdinRequest {
    const message = createBaseJobStdinRequest();
    message.jobId = object.jobId ?? "";
    message.data = object.data ?? new Uint8Array(0);
    return message;
  },
};

function createBaseJobStdin(): JobStdin {
  return { jobId: "", bytesWritten: "0" };
}

export const JobStdin: MessageFns<JobStdin> = {
  encode(message: JobStdin, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.jobId !== "") {
      writer.uint32(10).string(message.jobId);
    }
    if (message.bytesWritten !== "0") {
      writer.uint32(16).uint64(message.bytesWritten);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): JobStdin {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseJobStdin();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.jobId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.bytesWritten = reader.uint64().toString();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): JobStdin {
    return {
      jobId: isSet(object.jobId)
        ? globalThis.String(object.jobId)
        : isSet(object.job_id)
        ? globalThis.String(object.job_id)
        : "",
      bytesWritten: isSet(object.bytesWritten)
        ? globalThis.String(object.bytesWritten)
        : isSet(object.bytes_written)
        ? globalThis.String(object.bytes_written)
        : "0",
    };
  },

  toJSON(message: JobStdin): unknown {
    const obj: any = {};
    if (message.jobId !== "") {
      obj.jobId = message.jobId;
    }
    if (message.bytesWritten !== "0") {
      obj.bytesWritten = message.bytesWritten;
    }
    return obj;
  },

  create(base?: DeepPartial<JobStdin>): JobStdin {
    return JobStdin.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<JobStdin>): JobStdin {
    const message = createBaseJobStdin();
    message.jobId = object.jobId ?? "";
    message.bytesWritten = object.bytesWritten ?? "0";
    return message;
  },
};

function createBaseJobStdinClose(): JobStdinClose {
  return { jobId: "", closed: false };
}

export const JobStdinClose: MessageFns<JobStdinClose> = {
  encode(message: JobStdinClose, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.jobId !== "") {
      writer.uint32(10).string(message.jobId);
    }
    if (message.closed !== false) {
      writer.uint32(16).bool(message.closed);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): JobStdinClose {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseJobStdinClose();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.jobId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.closed = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): JobStdinClose {
    return {
      jobId: isSet(object.jobId)
        ? globalThis.String(object.jobId)
        : isSet(object.job_id)
        ? globalThis.String(object.job_id)
        : "",
      closed: isSet(object.closed) ? globalThis.Boolean(object.closed) : false,
    };
  },

  toJSON(message: JobStdinClose): unknown {
    const obj: any = {};
    if (message.jobId !== "") {
      obj.jobId = message.jobId;
    }
    if (message.closed !== false) {
      obj.closed = message.closed;
    }
    return obj;
  },

  create(base?: DeepPartial<JobStdinClose>): JobStdinClose {
    return JobStdinClose.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<JobStdinClose>): JobStdinClose {
    const message = createBaseJobStdinClose();
    message.jobId = object.jobId ?? "";
    message.closed = object.closed ?? false;
    return message;
  },
};

function createBaseServiceIdRequest(): ServiceIdRequest {
  return { serviceId: "" };
}

export const ServiceIdRequest: MessageFns<ServiceIdRequest> = {
  encode(message: ServiceIdRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.serviceId !== "") {
      writer.uint32(10).string(message.serviceId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ServiceIdRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseServiceIdRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.serviceId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ServiceIdRequest {
    return {
      serviceId: isSet(object.serviceId)
        ? globalThis.String(object.serviceId)
        : isSet(object.service_id)
        ? globalThis.String(object.service_id)
        : "",
    };
  },

  toJSON(message: ServiceIdRequest): unknown {
    const obj: any = {};
    if (message.serviceId !== "") {
      obj.serviceId = message.serviceId;
    }
    return obj;
  },

  create(base?: DeepPartial<ServiceIdRequest>): ServiceIdRequest {
    return ServiceIdRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ServiceIdRequest>): ServiceIdRequest {
    const message = createBaseServiceIdRequest();
    message.serviceId = object.serviceId ?? "";
    return message;
  },
};

function createBaseServiceDefinition(): ServiceDefinition {
  return { id: "", name: "", host: "", port: 0, protocol: "", description: "" };
}

export const ServiceDefinition: MessageFns<ServiceDefinition> = {
  encode(message: ServiceDefinition, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.name !== "") {
      writer.uint32(18).string(message.name);
    }
    if (message.host !== "") {
      writer.uint32(26).string(message.host);
    }
    if (message.port !== 0) {
      writer.uint32(32).uint32(message.port);
    }
    if (message.protocol !== "") {
      writer.uint32(42).string(message.protocol);
    }
    if (message.description !== "") {
      writer.uint32(50).string(message.description);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ServiceDefinition {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseServiceDefinition();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.host = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.port = reader.uint32();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.protocol = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.description = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ServiceDefinition {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      host: isSet(object.host) ? globalThis.String(object.host) : "",
      port: isSet(object.port) ? globalThis.Number(object.port) : 0,
      protocol: isSet(object.protocol) ? globalThis.String(object.protocol) : "",
      description: isSet(object.description) ? globalThis.String(object.description) : "",
    };
  },

  toJSON(message: ServiceDefinition): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.host !== "") {
      obj.host = message.host;
    }
    if (message.port !== 0) {
      obj.port = Math.round(message.port);
    }
    if (message.protocol !== "") {
      obj.protocol = message.protocol;
    }
    if (message.description !== "") {
      obj.description = message.description;
    }
    return obj;
  },

  create(base?: DeepPartial<ServiceDefinition>): ServiceDefinition {
    return ServiceDefinition.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ServiceDefinition>): ServiceDefinition {
    const message = createBaseServiceDefinition();
    message.id = object.id ?? "";
    message.name = object.name ?? "";
    message.host = object.host ?? "";
    message.port = object.port ?? 0;
    message.protocol = object.protocol ?? "";
    message.description = object.description ?? "";
    return message;
  },
};

function createBaseServiceList(): ServiceList {
  return { services: [] };
}

export const ServiceList: MessageFns<ServiceList> = {
  encode(message: ServiceList, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.services) {
      ServiceDefinition.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ServiceList {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseServiceList();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.services.push(ServiceDefinition.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ServiceList {
    return {
      services: globalThis.Array.isArray(object?.services)
        ? object.services.map((e: any) => ServiceDefinition.fromJSON(e))
        : [],
    };
  },

  toJSON(message: ServiceList): unknown {
    const obj: any = {};
    if (message.services?.length) {
      obj.services = message.services.map((e) => ServiceDefinition.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<ServiceList>): ServiceList {
    return ServiceList.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ServiceList>): ServiceList {
    const message = createBaseServiceList();
    message.services = object.services?.map((e) => ServiceDefinition.fromPartial(e)) || [];
    return message;
  },
};

function createBaseServiceCheck(): ServiceCheck {
  return { id: "", ok: false, latencyMs: "0", reason: "", hasReason: false };
}

export const ServiceCheck: MessageFns<ServiceCheck> = {
  encode(message: ServiceCheck, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.ok !== false) {
      writer.uint32(16).bool(message.ok);
    }
    if (message.latencyMs !== "0") {
      writer.uint32(24).uint64(message.latencyMs);
    }
    if (message.reason !== "") {
      writer.uint32(34).string(message.reason);
    }
    if (message.hasReason !== false) {
      writer.uint32(40).bool(message.hasReason);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ServiceCheck {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseServiceCheck();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.ok = reader.bool();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.latencyMs = reader.uint64().toString();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.reason = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.hasReason = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ServiceCheck {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      ok: isSet(object.ok) ? globalThis.Boolean(object.ok) : false,
      latencyMs: isSet(object.latencyMs)
        ? globalThis.String(object.latencyMs)
        : isSet(object.latency_ms)
        ? globalThis.String(object.latency_ms)
        : "0",
      reason: isSet(object.reason) ? globalThis.String(object.reason) : "",
      hasReason: isSet(object.hasReason)
        ? globalThis.Boolean(object.hasReason)
        : isSet(object.has_reason)
        ? globalThis.Boolean(object.has_reason)
        : false,
    };
  },

  toJSON(message: ServiceCheck): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.ok !== false) {
      obj.ok = message.ok;
    }
    if (message.latencyMs !== "0") {
      obj.latencyMs = message.latencyMs;
    }
    if (message.reason !== "") {
      obj.reason = message.reason;
    }
    if (message.hasReason !== false) {
      obj.hasReason = message.hasReason;
    }
    return obj;
  },

  create(base?: DeepPartial<ServiceCheck>): ServiceCheck {
    return ServiceCheck.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ServiceCheck>): ServiceCheck {
    const message = createBaseServiceCheck();
    message.id = object.id ?? "";
    message.ok = object.ok ?? false;
    message.latencyMs = object.latencyMs ?? "0";
    message.reason = object.reason ?? "";
    message.hasReason = object.hasReason ?? false;
    return message;
  },
};

function createBaseAuditEvent(): AuditEvent {
  return {
    subject: "",
    timestampMs: "0",
    nodeId: "",
    capability: "",
    action: "",
    resource: "",
    allowed: false,
    reason: "",
    runId: "",
    stepId: "",
    hasRunId: false,
    hasStepId: false,
  };
}

export const AuditEvent: MessageFns<AuditEvent> = {
  encode(message: AuditEvent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.subject !== "") {
      writer.uint32(10).string(message.subject);
    }
    if (message.timestampMs !== "0") {
      writer.uint32(16).uint64(message.timestampMs);
    }
    if (message.nodeId !== "") {
      writer.uint32(26).string(message.nodeId);
    }
    if (message.capability !== "") {
      writer.uint32(34).string(message.capability);
    }
    if (message.action !== "") {
      writer.uint32(42).string(message.action);
    }
    if (message.resource !== "") {
      writer.uint32(50).string(message.resource);
    }
    if (message.allowed !== false) {
      writer.uint32(56).bool(message.allowed);
    }
    if (message.reason !== "") {
      writer.uint32(66).string(message.reason);
    }
    if (message.runId !== "") {
      writer.uint32(74).string(message.runId);
    }
    if (message.stepId !== "") {
      writer.uint32(82).string(message.stepId);
    }
    if (message.hasRunId !== false) {
      writer.uint32(88).bool(message.hasRunId);
    }
    if (message.hasStepId !== false) {
      writer.uint32(96).bool(message.hasStepId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AuditEvent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAuditEvent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.subject = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.timestampMs = reader.uint64().toString();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.nodeId = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.capability = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.action = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.resource = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.allowed = reader.bool();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.reason = reader.string();
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.runId = reader.string();
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.stepId = reader.string();
          continue;
        }
        case 11: {
          if (tag !== 88) {
            break;
          }

          message.hasRunId = reader.bool();
          continue;
        }
        case 12: {
          if (tag !== 96) {
            break;
          }

          message.hasStepId = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AuditEvent {
    return {
      subject: isSet(object.subject) ? globalThis.String(object.subject) : "",
      timestampMs: isSet(object.timestampMs)
        ? globalThis.String(object.timestampMs)
        : isSet(object.timestamp_ms)
        ? globalThis.String(object.timestamp_ms)
        : "0",
      nodeId: isSet(object.nodeId)
        ? globalThis.String(object.nodeId)
        : isSet(object.node_id)
        ? globalThis.String(object.node_id)
        : "",
      capability: isSet(object.capability) ? globalThis.String(object.capability) : "",
      action: isSet(object.action) ? globalThis.String(object.action) : "",
      resource: isSet(object.resource) ? globalThis.String(object.resource) : "",
      allowed: isSet(object.allowed) ? globalThis.Boolean(object.allowed) : false,
      reason: isSet(object.reason) ? globalThis.String(object.reason) : "",
      runId: isSet(object.runId)
        ? globalThis.String(object.runId)
        : isSet(object.run_id)
        ? globalThis.String(object.run_id)
        : "",
      stepId: isSet(object.stepId)
        ? globalThis.String(object.stepId)
        : isSet(object.step_id)
        ? globalThis.String(object.step_id)
        : "",
      hasRunId: isSet(object.hasRunId)
        ? globalThis.Boolean(object.hasRunId)
        : isSet(object.has_run_id)
        ? globalThis.Boolean(object.has_run_id)
        : false,
      hasStepId: isSet(object.hasStepId)
        ? globalThis.Boolean(object.hasStepId)
        : isSet(object.has_step_id)
        ? globalThis.Boolean(object.has_step_id)
        : false,
    };
  },

  toJSON(message: AuditEvent): unknown {
    const obj: any = {};
    if (message.subject !== "") {
      obj.subject = message.subject;
    }
    if (message.timestampMs !== "0") {
      obj.timestampMs = message.timestampMs;
    }
    if (message.nodeId !== "") {
      obj.nodeId = message.nodeId;
    }
    if (message.capability !== "") {
      obj.capability = message.capability;
    }
    if (message.action !== "") {
      obj.action = message.action;
    }
    if (message.resource !== "") {
      obj.resource = message.resource;
    }
    if (message.allowed !== false) {
      obj.allowed = message.allowed;
    }
    if (message.reason !== "") {
      obj.reason = message.reason;
    }
    if (message.runId !== "") {
      obj.runId = message.runId;
    }
    if (message.stepId !== "") {
      obj.stepId = message.stepId;
    }
    if (message.hasRunId !== false) {
      obj.hasRunId = message.hasRunId;
    }
    if (message.hasStepId !== false) {
      obj.hasStepId = message.hasStepId;
    }
    return obj;
  },

  create(base?: DeepPartial<AuditEvent>): AuditEvent {
    return AuditEvent.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<AuditEvent>): AuditEvent {
    const message = createBaseAuditEvent();
    message.subject = object.subject ?? "";
    message.timestampMs = object.timestampMs ?? "0";
    message.nodeId = object.nodeId ?? "";
    message.capability = object.capability ?? "";
    message.action = object.action ?? "";
    message.resource = object.resource ?? "";
    message.allowed = object.allowed ?? false;
    message.reason = object.reason ?? "";
    message.runId = object.runId ?? "";
    message.stepId = object.stepId ?? "";
    message.hasRunId = object.hasRunId ?? false;
    message.hasStepId = object.hasStepId ?? false;
    return message;
  },
};

function createBaseAuditLog(): AuditLog {
  return { events: [] };
}

export const AuditLog: MessageFns<AuditLog> = {
  encode(message: AuditLog, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.events) {
      AuditEvent.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AuditLog {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAuditLog();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.events.push(AuditEvent.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AuditLog {
    return {
      events: globalThis.Array.isArray(object?.events) ? object.events.map((e: any) => AuditEvent.fromJSON(e)) : [],
    };
  },

  toJSON(message: AuditLog): unknown {
    const obj: any = {};
    if (message.events?.length) {
      obj.events = message.events.map((e) => AuditEvent.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<AuditLog>): AuditLog {
    return AuditLog.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<AuditLog>): AuditLog {
    const message = createBaseAuditLog();
    message.events = object.events?.map((e) => AuditEvent.fromPartial(e)) || [];
    return message;
  },
};

export type OperonRuntimeDefinition = typeof OperonRuntimeDefinition;
export const OperonRuntimeDefinition = {
  name: "OperonRuntime",
  fullName: "operon.runtime.v1.OperonRuntime",
  methods: {
    health: {
      name: "Health",
      requestType: HealthRequest as typeof HealthRequest,
      requestStream: false,
      responseType: HealthStatus as typeof HealthStatus,
      responseStream: false,
      options: {},
    },
    getNode: {
      name: "GetNode",
      requestType: GetNodeRequest as typeof GetNodeRequest,
      requestStream: false,
      responseType: NodeInfo as typeof NodeInfo,
      responseStream: false,
      options: {},
    },
    listCapabilities: {
      name: "ListCapabilities",
      requestType: ListCapabilitiesRequest as typeof ListCapabilitiesRequest,
      requestStream: false,
      responseType: CapabilityList as typeof CapabilityList,
      responseStream: false,
      options: {},
    },
    statFs: {
      name: "StatFs",
      requestType: FsPathRequest as typeof FsPathRequest,
      requestStream: false,
      responseType: FsStat as typeof FsStat,
      responseStream: false,
      options: {},
    },
    listFs: {
      name: "ListFs",
      requestType: FsPathRequest as typeof FsPathRequest,
      requestStream: false,
      responseType: FsList as typeof FsList,
      responseStream: false,
      options: {},
    },
    readFile: {
      name: "ReadFile",
      requestType: FsPathRequest as typeof FsPathRequest,
      requestStream: false,
      responseType: FileChunk as typeof FileChunk,
      responseStream: true,
      options: {},
    },
    writeFile: {
      name: "WriteFile",
      requestType: WriteFileRequest as typeof WriteFileRequest,
      requestStream: true,
      responseType: FsWrite as typeof FsWrite,
      responseStream: false,
      options: {},
    },
    runJob: {
      name: "RunJob",
      requestType: JobRunRequest as typeof JobRunRequest,
      requestStream: false,
      responseType: JobRecord as typeof JobRecord,
      responseStream: false,
      options: {},
    },
    getJob: {
      name: "GetJob",
      requestType: JobIdRequest as typeof JobIdRequest,
      requestStream: false,
      responseType: JobRecord as typeof JobRecord,
      responseStream: false,
      options: {},
    },
    listJobs: {
      name: "ListJobs",
      requestType: ListJobsRequest as typeof ListJobsRequest,
      requestStream: false,
      responseType: JobList as typeof JobList,
      responseStream: false,
      options: {},
    },
    streamJobLogs: {
      name: "StreamJobLogs",
      requestType: JobIdRequest as typeof JobIdRequest,
      requestStream: false,
      responseType: JobLog as typeof JobLog,
      responseStream: true,
      options: {},
    },
    writeJobStdin: {
      name: "WriteJobStdin",
      requestType: JobStdinRequest as typeof JobStdinRequest,
      requestStream: true,
      responseType: JobStdin as typeof JobStdin,
      responseStream: false,
      options: {},
    },
    closeJobStdin: {
      name: "CloseJobStdin",
      requestType: JobIdRequest as typeof JobIdRequest,
      requestStream: false,
      responseType: JobStdinClose as typeof JobStdinClose,
      responseStream: false,
      options: {},
    },
    cancelJob: {
      name: "CancelJob",
      requestType: JobCancelRequest as typeof JobCancelRequest,
      requestStream: false,
      responseType: JobRecord as typeof JobRecord,
      responseStream: false,
      options: {},
    },
    listServices: {
      name: "ListServices",
      requestType: ListServicesRequest as typeof ListServicesRequest,
      requestStream: false,
      responseType: ServiceList as typeof ServiceList,
      responseStream: false,
      options: {},
    },
    checkService: {
      name: "CheckService",
      requestType: ServiceIdRequest as typeof ServiceIdRequest,
      requestStream: false,
      responseType: ServiceCheck as typeof ServiceCheck,
      responseStream: false,
      options: {},
    },
    listAudit: {
      name: "ListAudit",
      requestType: ListAuditRequest as typeof ListAuditRequest,
      requestStream: false,
      responseType: AuditLog as typeof AuditLog,
      responseStream: false,
      options: {},
    },
  },
} as const;

export interface OperonRuntimeServiceImplementation<CallContextExt = {}> {
  health(request: HealthRequest, context: CallContext & CallContextExt): Promise<DeepPartial<HealthStatus>>;
  getNode(request: GetNodeRequest, context: CallContext & CallContextExt): Promise<DeepPartial<NodeInfo>>;
  listCapabilities(
    request: ListCapabilitiesRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<CapabilityList>>;
  statFs(request: FsPathRequest, context: CallContext & CallContextExt): Promise<DeepPartial<FsStat>>;
  listFs(request: FsPathRequest, context: CallContext & CallContextExt): Promise<DeepPartial<FsList>>;
  readFile(
    request: FsPathRequest,
    context: CallContext & CallContextExt,
  ): ServerStreamingMethodResult<DeepPartial<FileChunk>>;
  writeFile(
    request: AsyncIterable<WriteFileRequest>,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<FsWrite>>;
  runJob(request: JobRunRequest, context: CallContext & CallContextExt): Promise<DeepPartial<JobRecord>>;
  getJob(request: JobIdRequest, context: CallContext & CallContextExt): Promise<DeepPartial<JobRecord>>;
  listJobs(request: ListJobsRequest, context: CallContext & CallContextExt): Promise<DeepPartial<JobList>>;
  streamJobLogs(
    request: JobIdRequest,
    context: CallContext & CallContextExt,
  ): ServerStreamingMethodResult<DeepPartial<JobLog>>;
  writeJobStdin(
    request: AsyncIterable<JobStdinRequest>,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<JobStdin>>;
  closeJobStdin(request: JobIdRequest, context: CallContext & CallContextExt): Promise<DeepPartial<JobStdinClose>>;
  cancelJob(request: JobCancelRequest, context: CallContext & CallContextExt): Promise<DeepPartial<JobRecord>>;
  listServices(request: ListServicesRequest, context: CallContext & CallContextExt): Promise<DeepPartial<ServiceList>>;
  checkService(request: ServiceIdRequest, context: CallContext & CallContextExt): Promise<DeepPartial<ServiceCheck>>;
  listAudit(request: ListAuditRequest, context: CallContext & CallContextExt): Promise<DeepPartial<AuditLog>>;
}

export interface OperonRuntimeClient<CallOptionsExt = {}> {
  health(request: DeepPartial<HealthRequest>, options?: CallOptions & CallOptionsExt): Promise<HealthStatus>;
  getNode(request: DeepPartial<GetNodeRequest>, options?: CallOptions & CallOptionsExt): Promise<NodeInfo>;
  listCapabilities(
    request: DeepPartial<ListCapabilitiesRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<CapabilityList>;
  statFs(request: DeepPartial<FsPathRequest>, options?: CallOptions & CallOptionsExt): Promise<FsStat>;
  listFs(request: DeepPartial<FsPathRequest>, options?: CallOptions & CallOptionsExt): Promise<FsList>;
  readFile(request: DeepPartial<FsPathRequest>, options?: CallOptions & CallOptionsExt): AsyncIterable<FileChunk>;
  writeFile(
    request: AsyncIterable<DeepPartial<WriteFileRequest>>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<FsWrite>;
  runJob(request: DeepPartial<JobRunRequest>, options?: CallOptions & CallOptionsExt): Promise<JobRecord>;
  getJob(request: DeepPartial<JobIdRequest>, options?: CallOptions & CallOptionsExt): Promise<JobRecord>;
  listJobs(request: DeepPartial<ListJobsRequest>, options?: CallOptions & CallOptionsExt): Promise<JobList>;
  streamJobLogs(request: DeepPartial<JobIdRequest>, options?: CallOptions & CallOptionsExt): AsyncIterable<JobLog>;
  writeJobStdin(
    request: AsyncIterable<DeepPartial<JobStdinRequest>>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<JobStdin>;
  closeJobStdin(request: DeepPartial<JobIdRequest>, options?: CallOptions & CallOptionsExt): Promise<JobStdinClose>;
  cancelJob(request: DeepPartial<JobCancelRequest>, options?: CallOptions & CallOptionsExt): Promise<JobRecord>;
  listServices(request: DeepPartial<ListServicesRequest>, options?: CallOptions & CallOptionsExt): Promise<ServiceList>;
  checkService(request: DeepPartial<ServiceIdRequest>, options?: CallOptions & CallOptionsExt): Promise<ServiceCheck>;
  listAudit(request: DeepPartial<ListAuditRequest>, options?: CallOptions & CallOptionsExt): Promise<AuditLog>;
}

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from((globalThis as any).Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return (globalThis as any).Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export type ServerStreamingMethodResult<Response> = { [Symbol.asyncIterator](): AsyncIterator<Response, void> };

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
