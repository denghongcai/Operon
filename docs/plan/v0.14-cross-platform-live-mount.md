# v0.14 Cross-Platform Live Mount

Status: In progress.

## Goal

Make live mount a complete core Operon capability across Linux, macOS, and
Windows instead of treating non-Linux mount support as deferred convenience
work.

## Context

v0.13.7 documented macFUSE and WinFsp implementation strategy and chose a
pre-v1.0 Linux-only live mount boundary. v0.13.8 then extracted the first
platform-neutral `mount_core` boundary. That boundary is useful, but it is not
enough for the product goal: users should be able to expose an Operon
filesystem as a live local mount on all primary release platforms.

This phase supersedes the v0.13.7 Linux-only pre-v1.0 support decision. The
Core FS Protocol remains the authoritative filesystem API, but live mount is now
treated as a core adapter capability that must reach macOS and Windows.

## Scope

1. Shared mount adapter core.
   - Move reusable mount behavior out of the Linux FUSE adapter:
     path normalization, child validation, remote operation mapping, stat/list
     conversion, read-range/write-range behavior, truncation,
     mkdir/delete/rename semantics, error vocabulary, cancellation expectations,
     and common test fixtures.
   - Keep daemon policy, version/precondition checks, audit, and workspace
     containment authoritative on the daemon side. Mount adapters must not
     bypass Core FS Protocol semantics.
   - Keep platform adapters thin: they translate OS filesystem callbacks into
     shared mount-core operations.

2. macOS live mount.
   - Convert the current Linux-only FUSE adapter into a Unix FUSE adapter that
     supports Linux and macOS where the `fuser` crate and FUSE-T runtime are
     available.
   - Make `operon mount` available on macOS builds.
   - Add macOS-specific missing-runtime diagnostics for FUSE-T installation,
     network-volume permission, and backend limitations.
   - Use FUSE-T's FUSE-compatible path so the existing FUSE callback model can
     remain thin; do not build Finder shell polish or File Provider-style sync
     in this phase.

3. Windows live mount.
   - Add a native WinFsp adapter after the shared core and macOS adapter are
     working.
   - Prefer MIT-compatible bindings such as `winfsp_wrs` or direct FFI if the
     binding surface is too small. Do not add GPLv3 `winfsp` to the Apache-2.0
     workspace without an explicit license decision.
   - Make `operon mount` available on Windows builds when WinFsp support is
     compiled in.
   - Add missing-runtime diagnostics for the WinFsp driver/runtime and keep
     Windows filesystem semantics explicit instead of pretending they are POSIX.

4. CLI, doctor, docs, and release alignment.
   - Replace the current non-Linux `operon mount is only supported on Linux`
     error with platform-aware support and diagnostics.
   - Extend `operon doctor` to explain mount adapter status on Linux, macOS,
     and Windows.
   - Update README, PROTOCOL, runtime API docs, AGENTS guidance, and repo-local
     skills when the implemented support boundary changes.
   - Update release notes and validation scripts so public artifacts truthfully
     describe which live mount paths are supported, experimental, or blocked by
     missing platform runtime dependencies.

## Implementation Order

1. Plan and validation scaffolding.
   - Add this phase and wire a static validation script that checks the intended
     platform gates, docs, and CLI support wording.
   - Keep Linux mount validation green before changing adapter boundaries.

2. Shared mount-core expansion.
   - Add mount-core unit tests first for operation mapping, stat conversion,
     path/name validation, remote error classification, and cancellation
     behavior that can be exercised without a live kernel mount.
   - Move reusable logic out of the current Linux FUSE callback module only
     after tests fail against the current boundary.

3. Unix FUSE adapter.
   - Rename or split the Linux-specific adapter into a Unix FUSE adapter where
     practical.
   - Keep Linux behavior unchanged and validated by existing Linux live mount
     scripts.
   - Enable macOS compile/test paths with `fuser` and FUSE-T-oriented
     diagnostics.

4. macOS smoke path.
   - Add normal GitHub-hosted macOS compile and unit validation.
   - Add a manual or self-hosted macOS live mount smoke plan if hosted runners
     cannot install or use FUSE-T reliably.
   - Smoke cases: mount, list, stat, read, random read, write, truncate, mkdir,
     rename, delete, permission denied, unmount, and daemon disconnect.

5. Windows WinFsp adapter.
   - Add the Windows adapter behind explicit Windows `cfg` gates.
   - Keep WinFsp dependency optional or target-scoped.
   - Add unit tests for UTF-16 path handling, Windows error mapping, read/write
     operation dispatch, delete/rename semantics, missing WinFsp runtime
     diagnostics, and unmount/cancellation behavior.

6. Windows smoke path.
   - Add a Windows CI or temporary Actions smoke that installs WinFsp only after
     the dependency and runner permissions are proven.
   - Smoke cases: mount or drive exposure, list, stat, read, random read,
     write, truncate, mkdir, rename, delete, permission denied, unmount, and
     daemon/service stop.

7. Public release hardening.
   - Align versions, docs, CLI help, `operon doctor`, release workflows, and
     release artifact verification before publishing a release that claims
     cross-platform live mount support.
   - Run README Quickstart and release artifact validation against the public
     release after publication.

## Non-Goals

- No offline file sync engine.
- No global workspace replication.
- No File Provider, Explorer shell extension, Finder badge, preview, or shell
  polish.
- No conflict resolution beyond the existing Core FS Protocol version and
  precondition semantics.
- No Windows ACL editing or alternate data stream support in the first Windows
  adapter.
- No POSIX uid/gid/mode fidelity guarantee on Windows.
- No macFUSE kernel-extension security downgrade as the default documented
  path.
- No transport, VPN, relay, NAT traversal, or network mesh responsibility.

## Acceptance Criteria

- `operon mount` supports Linux, macOS, and Windows release builds with
  platform-aware diagnostics.
- Linux live mount behavior remains unchanged and validated by existing Linux
  live mount scripts.
- macOS has a real live mount adapter path, compile/unit validation in CI, and
  a documented live smoke gate.
- Windows has a real WinFsp native adapter path, compile/unit validation in CI,
  and a documented or automated live smoke gate.
- Missing macFUSE or WinFsp runtime dependencies produce actionable CLI and
  `operon doctor` diagnostics.
- Shared mount-core tests cover platform-neutral path, stat, read/write range,
  truncate, mkdir, delete, rename, error, and cancellation behavior.
- README, PROTOCOL, runtime API docs, AGENTS guidance, repo-local skills, and
  release docs match the implemented support boundary.

## Validation

Local validation must include:

- `cargo fmt --all`
- `cargo test -p operon-mount --locked`
- target checks for Linux, macOS, and Windows mount code paths where available
- existing Linux live mount scripts
- the new v0.14 validation script
- `scripts/ci/run-validations.sh core`
- `scripts/ci/run-validations.sh runtime`

CI validation must include:

- Linux live mount validation through the existing `linux-system` group.
- macOS compile and non-live mount unit validation.
- Windows compile and non-live mount unit validation.
- Temporary or permanent Actions smoke jobs for macFUSE and WinFsp once runner
  dependencies are proven reliable.

Release validation must include:

- CI and CodeQL on the release commit.
- tag-triggered release artifact generation.
- post-publication release artifact verification.
- README Quickstart validation against the public release tag.

## Remaining

- Shared mount-core operation mapping, platform-neutral error classification,
  Unix FUSE adapter gating, macOS non-live compile validation, Windows native
  WinFsp adapter implementation, CLI mount dispatch, doctor diagnostics, CI
  validation wiring, and macOS release build dependency wiring are implemented.
- Rust crate versions, TypeScript SDK package metadata, CLI version output, and
  `PROTOCOL_VERSION` are aligned to `0.14.0` / `v0.14.0` for the release line.
- Manual Actions live-smoke workflow scaffolding is present for macOS FUSE-T
  and Windows WinFsp through `.github/workflows/v0.14-live-mount-smoke.yml`.
- Linux live mount validation is green after preserving inode mappings across
  FUSE rename callbacks; this keeps kernel dentries usable for post-rename
  `stat` and `unlink` operations.
- Local validation has passed for `scripts/ci/run-validations.sh core`,
  `scripts/ci/run-validations.sh runtime`, and
  `scripts/ci/run-validations.sh linux-system`.
- The first remote CI pass found and fixed platform validation wiring issues:
  macOS non-live CLI tests now use the `operon-mount/macos-no-mount` feature,
  Windows platform smoke installs WinFsp before compiling the workspace, stale
  Linux-only validation assertions now match the cross-platform mount boundary,
  and live-smoke scripts prebuild daemon/CLI binaries before waiting for mount
  exposure.
- The second remote CI pass is green for Rust, TypeScript, macOS/Windows
  platform smoke, and all consolidated validation groups. The Windows live
  smoke still showed the mount CLI process exiting before the WinFsp drive was
  visible; the current checkpoint hardens mount-session shutdown handling so a
  failed Ctrl-C handler install or disconnected shutdown channel does not
  terminate the mount process in non-interactive runner contexts, and expands
  the Windows live-smoke script to print daemon, mount, process, and WinFsp
  service diagnostics on failure.
- Windows live-smoke diagnostics then isolated the next failure to mount-process
  termination before any CLI stdout/stderr, with the daemon and WinFsp Launcher
  service both healthy. The Windows adapter now calls `winfsp_wrs::init()`
  before using WinFsp APIs so the delayed WinFsp DLL is loaded from the
  installed WinFsp directory instead of crashing at first API touch.
- The next Windows live-smoke pass confirmed that process termination still
  happened before CLI stdout/stderr. The root cause is the missing MSVC
  delayload linker wiring required by `winfsp_wrs`: without
  `/DELAYLOAD:winfsp-x64.dll`, the Windows loader attempts to resolve the
  WinFsp DLL before `main`, so `winfsp_wrs::init()` never gets a chance to load
  the installed runtime DLL. `operon-cli` and `operon-mount` now use
  `winfsp_wrs_build` build scripts on Windows to configure the delayed WinFsp
  DLL load path.
- The follow-up Windows live-smoke run reached a live mounted process and CLI
  output, but the mounted drive did not expose `seed.txt`. The Windows adapter
  now has opt-in `OPERON_MOUNT_TRACE` callback diagnostics and the Windows live
  smoke prints drive/root directory state so the next run can distinguish
  WinFsp mount visibility from directory callback behavior.
- The next Windows live-smoke diagnostics confirmed that `O:\` appears in
  `fsutil fsinfo drives`, but `dir O:\` returns `Incorrect function`. The
  smoke diagnostics now tolerate native diagnostic command failures and print
  daemon/mount logs before probing the mounted drive so callback trace output is
  preserved.
- The current Windows live-smoke diagnostic checkpoint adds tracing for the
  WinFsp volume-info and dispatcher-stopped callbacks, and records `mountvol`
  plus `fsutil fsinfo drivetype/volumeinfo` output. This should distinguish
  mount-manager exposure from callback dispatch and volume metadata failures
  before changing adapter behavior.
- Windows live-smoke diagnostics showed the drive letter is registered as a
  fixed drive, but volume information fails before `get_volume_info` reaches the
  adapter. The Windows WinFsp volume parameters now enable double buffering to
  match the more conservative default used by the `winfsp` Rust host wrapper.
- The manual live-smoke workflow now has per-job timeouts, and the macOS smoke
  script has a watchdog that dumps daemon, mount, process, and mounted-volume
  state before terminating a hung run.
- After double buffering did not change Windows live-smoke behavior, the
  Windows smoke build now enables a diagnostic-only `operon-mount/winfsp-debug`
  feature that forwards `winfsp_wrs/debug` so the next failed run can include
  WinFsp dispatcher/debug output without changing normal release builds.
- WinFsp debug output showed root directory opens arrive at the adapter as
  `Create "" FILE_OPEN` and were returning `STATUS_INVALID_DEVICE_REQUEST`
  before directory enumeration. The Windows adapter `create` path now stats and
  opens existing paths before falling back to mkdir/create-file behavior for
  missing paths.
- The next Windows live-smoke run still returned `STATUS_INVALID_DEVICE_REQUEST`
  for root opens while `OPERON_MOUNT_TRACE` showed no adapter `create` or
  `open` callback entry. The Windows adapter now exposes `CreateEx` as well as
  `Create`, delegating both paths to the same existing-path open-or-create
  logic required by newer WinFsp dispatch.
- Follow-up CI validation exposed a long-standing script fragility: completion
  generation was piped directly into `grep -q`, so `grep` could close the pipe
  early and trigger a `clap_complete` broken-pipe panic. The v0.8.1 integration
  coverage and v0.8 agent-skills validation scripts now write completion output
  to temporary files before asserting expected content.
- The latest manual live-smoke run confirmed baseline CI is green, while
  Windows still returns `STATUS_INVALID_DEVICE_REQUEST` for root opens before
  any adapter callback trace reaches stderr, and macOS prints `mount did not
  expose seed file` without enough immediate diagnostics. The next checkpoint
  makes macOS failure cleanup bounded and diagnostic-first, reduces Windows
  seed-exposure wait time, logs Windows callback flags and callback entry before
  remote filesystem calls, and narrows Windows creation dispatch to the
  `CreateEx` callback surface used by newer WinFsp examples.
- Follow-up live-smoke evidence confirmed the Windows failure happens before
  any adapter callback despite the expected callback flags being present. The
  same run also showed the macOS smoke script could continue after a diagnostic
  dump because `dump_diagnostics` disabled `errexit` globally; diagnostics now
  run in a subshell so a missing seed file remains a hard failure instead of a
  false pass.
- The next Windows checkpoint aligns the WinFsp adapter with conservative
  official sample-style volume/interface defaults after diagnostics showed
  callback flags were registered but WinFsp still rejected root opens before
  adapter dispatch. Windows now exposes the base `Create` callback instead of
  `CreateEx`, uses case-insensitive search, persistent ACLs,
  post-cleanup-when-modified-only, and an explicit volume creation time while
  retaining double buffering.
- Re-checking the local WinFsp Rust bindings showed `winfsp_wrs_sys` includes
  the `CreateEx` interface slot and the newer `winfsp` wrapper routes create
  dispatch through a `create_ex` trampoline. Because live-smoke logs showed
  WinFsp returning `STATUS_INVALID_DEVICE_REQUEST` before any adapter callback
  while `CreateEx` was disabled, the Windows adapter now exposes both `Create`
  and `CreateEx`, with `CreateEx` delegated to the same existing-path
  open-or-create logic.
- The manual live-smoke workflow now accepts `platform=all|macos|windows` so
  the hosted-runner macOS FSKit blocker does not prevent targeted Windows
  WinFsp validation after a Windows adapter fix.
- The latest macOS live-smoke failure now has a narrower boundary: the daemon
  and mount process stay alive, but the mountpoint never appears in the system
  mount table and the CLI never reaches the post-mount output. The macOS smoke
  script now tries to load the macFUSE kernel extension, records macFUSE/kext
  readiness diagnostics, and enables Unix mount trace points around remote
  connection, root stat, and `fuser::spawn_mount2` so the next run identifies
  whether the hang is macFUSE runtime readiness or FUSE handshake.
- Follow-up macOS evidence showed hosted runners install macFUSE 5.2.0 on
  macOS 15.7.4 without loading the kernel extension, and `spawn_mount2` hangs
  before completing the mount. The macOS adapter now selects macFUSE's
  `backend=fskit` mount option by default on macOS 15.4+ while preserving
  `OPERON_MOUNT_MACOS_BACKEND=kernel` for explicit kernel-backend validation.
- FSKit follow-up checks identified the next macOS smoke bug: macFUSE FSKit
  does not support mount points outside `/Volumes`, but the smoke script used a
  temporary `/var/folders/.../mount` directory. The macOS smoke now mounts under
  `/Volumes`, and the adapter rejects FSKit mount requests outside `/Volumes`
  with an actionable error instead of entering a hung mount handshake.
- The `/Volumes` FSKit smoke still hung in `fuser::spawn_mount2`, so the next
  checkpoint explicitly refreshes macFUSE's file-system-extension component via
  the macFUSE CLI and dumps recent FSKit/LiveFS/macFUSE unified logs when the
  mount does not appear. This targets the FSKit service-registration/XPC layer
  rather than remote filesystem behavior.
- The refreshed FSKit smoke reached the macOS FSKit service layer, but hosted
  runner logs report `Hello FSClient! entitlement no`, followed by macFUSE
  daemon mount and server-advertise failures. The remaining macOS blocker is
  now the hosted-runner FSKit entitlement/registration boundary, not Operon
  remote filesystem dispatch. Kernel-backend validation still requires a host
  where the macFUSE kernel extension is approved and loaded.
- Targeted Windows live-smoke evidence showed `O:\` was registered as a fixed
  drive, but root access still returned `Incorrect function` and WinFsp debug
  output returned `STATUS_INVALID_DEVICE_REQUEST` before any adapter callback
  trace reached stderr, even with both `Create` and `CreateEx` callback flags
  present. The Windows adapter now bypasses the `winfsp_wrs` high-level
  filesystem host and registers the `FSP_FILE_SYSTEM_INTERFACE` directly
  through the MIT-compatible `winfsp_wrs_sys` bindings, with direct callback
  entry tracing and explicit dispatcher/context/interface cleanup.
- The first direct-interface Windows smoke still registered `O:\` as a fixed
  drive but did not enter any adapter callback; `fsutil volumeinfo O:\` and
  `dir O:\` returned `Incorrect function`. The direct WinFsp path now logs
  `FspFileSystemCreate`, `FspFileSystemSetMountPoint`, and
  `FspFileSystemStartDispatcher` status and enables WinFsp debug logging when
  the diagnostic `operon-mount/winfsp-debug` feature is active.
- Direct-interface diagnostics then confirmed `FspFileSystemCreate`, mount
  point registration, dispatcher startup, debug logging, and all key interface
  slots were present, but WinFsp returned `STATUS_INVALID_DEVICE_REQUEST`
  before entering `Create`, `CreateEx`, or `Open`. Re-checking WinFsp v2.1
  `FspFileSystemOpCreate` showed it rejects create/open dispatch unless
  `Create/CreateEx`, `Open`, and `Overwrite/OverwriteEx` are all registered.
  The Windows adapter now registers an `Overwrite` callback and the validation
  script asserts that the required slot remains present.
- Windows live smoke passed on GitHub-hosted `windows-latest` for commit
  `b14a4bd` in run `25339076348`, covering drive exposure, read, write,
  truncate, mkdir, rename, delete, remote read-back, and cleanup through the
  WinFsp adapter.
- Current macOS live smoke on GitHub-hosted `macos-latest` still fails at the
  macFUSE FSKit/LiveFS service boundary for commit `b14a4bd` in run
  `25339408571`: Operon reaches `spawn_mount2_start`, while unified logs report
  an unentitled FSKit client followed by macFUSE daemon mount and
  server-advertise failures. The manual live-smoke workflow now exposes a
  `macos_backend=fskit|kernel` input so validation can target FSKit on hosted
  runners or the kernel backend on a macOS host where the macFUSE kernel
  extension is approved and loaded.
- A follow-up GitHub-hosted `macos_backend=kernel` run for commit `9d3c4df` in
  run `25340391127` failed during the macOS smoke step after Homebrew installed
  macFUSE and warned that a user-approved kernel extension is required. GitHub's
  job log did not include the failing smoke step body, so the manual
  live-smoke workflow now tees macOS smoke stdout/stderr into a runner-temp log,
  prints the smoke exit code, and uploads that log as an Actions artifact on
  success or failure before more kernel-backend validation attempts.
- The first artifact-backed `macos_backend=kernel` run for commit `4160b0c` in
  run `25340798030` confirmed the kernel extension remains unloaded on
  GitHub-hosted macOS, the mount never exposes the seed file, and Operon reaches
  `spawn_mount2_start`. That run also exposed a smoke-script cleanup bug:
  after diagnostics, cleanup waited without a bound for the stuck mount process
  until the job timeout cancelled the run. The macOS smoke script now uses a
  shorter default watchdog and bounded cleanup waits so future diagnostic runs
  return promptly and keep artifact upload reliable.
- The follow-up artifact-backed `macos_backend=kernel` run for commit `1ed85f2`
  in run `25341745841` now fails cleanly instead of timing out: the job log and
  uploaded artifact show `macFUSE kernel extension is not loaded`, `mount did
  not expose seed file`, and `spawn_mount2_start`, followed by
  `macOS live mount smoke exit code: 1`. This confirms the remaining hosted
  macOS blocker is environment-level macFUSE runtime availability, while the
  diagnostic workflow and cleanup path are now usable.
- The earlier macFUSE manual live-smoke workflow added a
  `macos_runner=hosted|self-hosted-macfuse` input. That lane is now superseded
  by the FUSE-T `macos_runner=hosted|self-hosted-fuse-t` workflow path.
- The repository Actions runner registry currently reports `total_count: 0`,
  so there is no matching self-hosted macOS/macFUSE runner to consume the
  `self-hosted-macfuse` lane. The superseded lane was not dispatched to avoid creating a
  queued run that cannot execute.
- The earlier `scripts/preflight-v0.14-macos-macfuse-host.sh` self-hosted
  runtime check is superseded by
  `scripts/preflight-v0.14-macos-fuse-t-host.sh`, which verifies FUSE-T and
  `pkg-config fuse` before the longer live smoke starts.
- `docs/plan/v0.14-macos-live-smoke-runbook.md` records the handoff procedure
  for the remaining macOS release gate: FUSE-T preflight command, workflow
  dispatch command, success evidence, and
  failure-log handling.
- The tag-triggered draft release workflow now runs
  `scripts/verify-v0.14-release-gates.sh` before artifact builds. For `v0.14*`
  tags, that guard requires a successful macOS FUSE-T live mount job in the
  `v0.14 Live Mount Smoke` workflow on the exact release commit.
- FUSE-T replaces macFUSE as the active macOS live-smoke runtime after
  GitHub-hosted macFUSE FSKit and kernel-backend attempts proved blocked by
  host runtime requirements rather than Operon filesystem dispatch. The macOS
  adapter still uses the existing `fuser` callback path, but it now defaults to
  `backend=nfs`, and the workflow installs FUSE-T through
  `macos-fuse-t/homebrew-cask/fuse-t`.
- FUSE-T's upstream README describes it as a kext-less macOS FUSE
  implementation that uses a local NFS server, keeps libfuse-compatible
  headers, and supports installation with
  `brew install macos-fuse-t/homebrew-cask/fuse-t`; that matches Operon's goal
  of reusing the current FUSE adapter without adding macFUSE kernel-extension
  setup. Source: <https://github.com/macos-fuse-t/fuse-t>.
- The v0.14 manual live-smoke workflow now targets
  `macos_runner=hosted|self-hosted-fuse-t` with
  `macos_backend=nfs|smb|fskit` and optional
  `macos_options=<comma-separated FUSE-T -o options>`; the release gate accepts
  a successful macOS FUSE-T hosted or self-hosted job on the exact release
  commit.
- First hosted FUSE-T attempt for commit `1c086ae` in run `25355056996`
  installed FUSE-T 1.2.1 successfully and found `/usr/local/lib/libfuse-t.dylib`,
  but the installer did not expose `fuse.h` on the runner. Because `fuser`
  only needs `pkg-config fuse` link metadata at build time, the install helper
  now generates link-only compatibility metadata when headers are absent.
- Follow-up hosted FUSE-T attempt for commit `a3409c6` in run `25355152823`
  reached the smoke script, but the debug `operon` binary aborted with
  `dyld: Library not loaded: @rpath/libfuse-t.dylib` because the generated
  compatibility metadata did not embed an rpath. The install helper now emits
  `-Wl,-rpath,<fuse-t-libdir>` and exports `DYLD_LIBRARY_PATH` for smoke
  commands.
- The next hosted NFS attempt for commit `acc1e20` in run `25355546910`
  reached `spawn_mount2_ok`, so the Operon FUSE adapter and FUSE-T userspace
  mount path started. The mount did not expose the seed file, the macOS mount
  table did not show the volume, and unified logs reported
  `nfs_connect: socket connect taking a while for fuse-t:/...`. The next
  checkpoint adds deeper FUSE-T pkg-config, process, and socket diagnostics and
  also tests the SMB backend to separate Operon adapter behavior from the
  FUSE-T NFS network-volume path.
- The hosted SMB attempt for commit `acc1e20` in run `25355950126` also
  reached `spawn_mount2_ok` but did not expose the seed file, matching the NFS
  behavior at the point where FUSE-T should publish a macOS network volume.
  The install helper now seeds `PKG_CONFIG_PATH` with both `/usr/local` and
  `/Library/Application Support/fuse-t/pkgconfig` before probing FUSE-T metadata
  so the next hosted run can confirm the exact upstream cflags/libs in use.
- Final hosted NFS diagnostic run for commit `189be7e` in run `25356391309`
  confirmed the installed FUSE-T metadata is valid: `fuse-t.pc` reports
  version `1.2.1`, `-I/usr/local/include/fuse`, and
  `-L/usr/local/lib -Wl,-rpath,/usr/local/lib -lfuse-t`. The smoke still did
  not expose the seed file after `spawn_mount2_ok`. Process/socket diagnostics
  showed `/sbin/mount_nfs` stuck on
  `fuse-t:/operon-v014-macos-live-mount-*`, while the FUSE-T NFS server process
  was listening on `127.0.0.1:<port>` with a `CLOSE_WAIT` connection. This
  narrows the GitHub-hosted blocker to the FUSE-T/macOS network-volume
  connection path after Operon's mount adapter has started successfully.
- The FUSE-T wiki review added an explicit macOS `-o` option passthrough
  through `OPERON_MOUNT_MACOS_OPTIONS`, with tests for backend-plus-extra
  option construction and rejection of raw `-d`/`-l` style arguments. The live
  smoke now echoes those options and includes `~/Library/Logs/fuse-t` in failure
  diagnostics. The wiki's NFS listen-address flag remains intentionally
  unimplemented until it can be mapped safely through `fuser`, because raw
  custom values are passed as `-o <option>` rather than standalone arguments.
- Add a hosted macOS reference probe that clones and builds
  `https://github.com/macos-fuse-t/fuse-zip`, mounts a seed ZIP through the same
  FUSE-T runtime, checks seed-file read visibility, and uploads FUSE-T logs on
  failure. This is a control test for whether GitHub-hosted macOS can publish
  any FUSE-T mount independent of Operon's adapter; it intentionally does not
  make third-party fuse-zip write-back semantics part of the hosted-runner
  publication verdict.
- The first fuse-zip probe dispatch for commit `4b847e0` did not reach the
  mount path because fuse-zip's `make release` target dropped the FUSE include
  directory and failed with `fatal error: 'fuse.h' file not found`. The probe
  now builds `all doc` with explicit `pkg-config --cflags fuse libzip` and
  `pkg-config --libs fuse libzip` values so the next run tests the hosted
  FUSE-T mount behavior rather than the upstream Makefile defaults.
- The follow-up fuse-zip probe dispatch for commit `78d1061` in run
  `25358498739` compiled fuse-zip successfully and reached the mounted
  `seed.txt` read path. It failed only after the probe tried to assert that a
  newly written `new.txt` was persisted back into the source ZIP. The probe is
  now narrowed to its intended control purpose: prove FUSE-T hosted mount
  publication and read visibility with an independent FUSE implementation.
- The next fuse-zip probe dispatch for commit `f13e32b` in run `25358898425`
  printed `v0.14 macOS FUSE-T fuse-zip probe passed`, proving hosted FUSE-T
  read exposure with fuse-zip. The job was cancelled only because the probe's
  background shell watchdog left an orphan `sleep` process holding the Actions
  output pipe open after success. The probe watchdog now uses a single `perl`
  timer process so cleanup can terminate it without orphaning a child process.
- The corrected fuse-zip probe passed on GitHub-hosted `macos-latest` for
  commit `5f0a0bc` in run `25359068416` in 43 seconds. This proves the hosted
  runner can publish and read at least one FUSE-T NFS mount through an
  independent libfuse implementation, so the remaining Operon macOS live-smoke
  failure is no longer a blanket hosted-runner FUSE-T limitation. The next
  macOS checkpoint should compare Operon's `fuser` callback/mount option path
  against the working fuse-zip/libfuse path.
- Operon's Unix FUSE adapter now mirrors the low-risk fuse-zip compatibility
  callbacks needed by macOS/FUSE-T publication: known-inode `access()` returns
  success, `fsyncdir()` returns success, `statfs()` reports stable non-zero
  capacity metadata, and `OPERON_MOUNT_TRACE` logs lookup/getattr/open/read/
  write/readdir/access/statfs/fsyncdir callback entry. This keeps daemon-side
  policy authoritative while avoiding `ENOSYS` for callbacks that FUSE-T's NFS
  bridge may issue before exposing the mount to macOS clients.
- The Operon macOS live-smoke watchdog now also mirrors the fuse-zip probe's
  single-process `perl` timer instead of using a background shell plus `sleep`.
  This avoids orphaned sleep processes holding the Actions output pipe open and
  keeps hosted failure diagnostics and artifact upload bounded while the
  remaining mount behavior is debugged.
- The hosted Operon run for commit `aa8d3bf` in run `25359328268` showed
  `spawn_mount2_ok`, FUSE-T NFS server startup, and no `OPERON_MOUNT_TRACE`
  FUSE callback entry before `mount_nfs` stalled with a `CLOSE_WAIT`
  connection. Re-checking `fuser` 0.17 showed non-Linux session loops reject
  `n_threads != 1`; Operon had been setting four FUSE threads on every
  platform. The Unix mount session now uses four FUSE threads only on Linux and
  one FUSE thread on macOS, matching `fuser`'s supported platform boundary.
- The follow-up hosted Operon run for commit `4f764bd` in run `25359740331`
  reached live FUSE callbacks (`statfs` and root `getattr`) with
  `n_threads: 1`, then FUSE-T closed the NFS bridge before directory lookup or
  read visibility. The Unix FUSE adapter now mirrors more of fuse-zip's early
  macOS callback surface by tracing init/destroy/opendir/releasedir, returning
  a non-zero `statfs` fragment size, and responding to xattr probes with an
  empty/no-xattr result instead of falling through to default `ENOSYS`.
- The hosted Operon run for commit `9533415` in run `25359898168` still closed
  the FUSE-T NFS bridge after root `statfs/getattr` and an explicit
  `destroy`. To reduce the remaining difference from the successful fuse-zip
  control, macOS now uses only FUSE-T-specific mount options
  (`backend=<nfs|smb|fskit>` plus explicit extras) while Linux keeps Operon's
  `fsname`, `subtype`, `nodev`, `nosuid`, and `noexec` FUSE options.
- The hosted Operon run for commit `20512bc` in run `25360049773` still closed
  the FUSE-T NFS bridge immediately after root `statfs/getattr`, so the
  remaining issue is not the earlier Operon mount option set. The next
  fuse-zip-alignment checkpoint reports FUSE attributes with 512-byte stat
  blocks like fuse-zip and fuser's hello example, reports macOS file ownership
  as the mounting user instead of root, and expands macOS smoke diagnostics to
  include `mount_nfs` and NFS unified logs plus `nfsstat -m`.
- The hosted Operon run for commit `adb1234` in run `25360306048` confirmed
  those uid/gid and block-size changes reached macOS (`uid=501`, `gid=20`,
  `blksize=512`), but FUSE-T still destroyed the session after the second root
  `statfs/getattr`. The next checkpoint removes the remaining empty-directory
  attribute difference from fuse-zip/fuser hello by reporting directory
  `blocks=0` and aligns `statfs` free-inode reporting to fuse-zip's zero value.
- The hosted Operon run for commit `f48d451` in run `25360430340` confirmed the
  root attributes now match the fuser hello and fuse-zip shape
  (`blocks=0`, `blksize=512`, mounter uid/gid), but the FUSE-T NFS bridge still
  destroys the session before `lookup` or `readdir`. The next diagnostic step
  adds a standalone hosted macOS `fuser` hello probe to separate Operon
  filesystem semantics from the `fuser` low-level session path. If that probe
  fails the same way, the macOS adapter should move to a libfuse-style high-level
  mount path instead of continuing callback micro-adjustments.
- The standalone hosted macOS `fuser` hello probe for commit `8cc5b55` in run
  `25360630179` failed with the same shape as Operon: the minimal filesystem
  received `init`, two root `statfs/getattr` rounds, and `destroy`, while
  FUSE-T logged `Connection closed` and no `lookup` or `readdir` reached the
  filesystem. This confirms the remaining macOS blocker is the `fuser` low-level
  session path against FUSE-T's NFS bridge, not Operon's remote filesystem
  semantics. The next implementation checkpoint should replace the macOS mount
  adapter with a fuse-zip-style libfuse high-level path (`fuse_setup` /
  `fuse_loop`) while keeping Linux on `fuser`.
- The follow-up diagnostic branch `investigate-macos-fuser-fuset` adds a
  workflow-controlled `fuser` hello experiment that locally patches fuser
  0.17's macOS `INIT_FLAGS` from unconditional `ASYNC_READ |
  CASE_INSENSITIVE | VOL_RENAME | XTIMES` to `ASYNC_READ` only. This tests the
  narrower hypothesis that fuser's macOS low-level handshake advertises
  capabilities that FUSE-T's NFS bridge/libfuse high-level path only enables
  when the corresponding filesystem operations exist.
- Hosted run `25375413405` tested that patched fuser hello experiment. The
  negotiated FUSE-T flags changed from the original `0xe0000001` to
  `0x40000001`, proving `CASE_INSENSITIVE` and `XTIMES` were removed, but the
  probe still failed with the same `init` -> root `statfs/getattr` -> `destroy`
  sequence and FUSE-T `Connection closed`. The remaining bit 30 comes from
  fuser's low-level `Init::reply` OR-ing `FUSE_INIT_EXT`, which collides with
  macOS `FUSE_VOL_RENAME` in the 7.19 handshake shape used by FUSE-T. The next
  diagnostic cut, if needed, should patch that `FUSE_INIT_EXT` behavior; the
  product implementation direction still points toward a macOS-specific libfuse
  high-level adapter instead of more Operon callback tuning.
- The next diagnostic cut vendors `fuser` 0.17.0 under
  `vendor/fuser-0.17.0-operon` and applies the macOS handshake patch directly:
  macOS default init flags stay at `FUSE_ASYNC_READ`, and macOS init replies no
  longer OR in Linux `FUSE_INIT_EXT`. This keeps Linux behavior unchanged while
  testing whether FUSE-T's NFS bridge can proceed past root `statfs/getattr`
  when the low-level reply no longer advertises the bit that macOS interprets
  as `FUSE_VOL_RENAME`.
- Hosted run `25375851519` tested the vendored fuser handshake patch. FUSE-T
  negotiated only `flags=0x00000001`, proving the macOS init reply no longer
  advertises `CASE_INSENSITIVE`, `VOL_RENAME`, `XTIMES`, or the colliding
  `FUSE_INIT_EXT` bit. The probe still failed with the same early
  `Connection closed` and the same `init` -> root `statfs/getattr` -> `destroy`
  callback sequence. This rules out fuser init flag advertisement as the
  sufficient root cause and leaves the remaining incompatibility in fuser's
  low-level session/mount path against FUSE-T's NFS bridge. Further fuser
  micro-patches should stop unless a new lower-level trace identifies a
  specific request/reply encoding bug; the practical implementation path is a
  macOS-specific libfuse high-level adapter.
- Hosted run `25376060153` enabled fuser's default callback logging in the
  minimal hello probe. It showed no hidden `[Not Implemented]` callback before
  disconnect; the only additional fuser-side error was `Short read of FUSE
  request header (0 < 40)` after FUSE-T closed the connection. Hosted run
  `25376436969` then proved a C/libfuse low-level hello filesystem succeeds on
  the same FUSE-T NFS hosted runner. Comparing fuser with FUSE-T libfuse source
  identified a sharper difference: fuser's macOS libfuse2 mount path used
  legacy `fuse_mount_compat25()` and received a raw fd, while successful
  libfuse low-level code uses current `fuse_mount()` and keeps the `fuse_chan`
  monitor/callback state. The next fuser patch switches macOS to
  `fuse_mount()` / `fuse_chan_fd()` while leaving Linux on the existing
  `fuse_mount_compat25()` path.
- The first current-`fuse_mount()` patch reached a compile-only failure on
  hosted run `25376658909`: fuser moves mount state into its background session
  thread, so storing a raw `*mut c_void` channel in `MountImpl` violated Rust's
  `Send` bound before the runtime hypothesis could be tested. The follow-up
  patch keeps the same libfuse channel path but stores the channel as an opaque
  integer and casts it back only for `fuse_unmount()`, preserving the minimal
  experiment while satisfying fuser's thread boundary.
- Hosted run `25376827613` then confirmed the patched fuser hello compiled and
  mounted through FUSE-T, but it still failed after the initial
  `statfs/getattr` sequence with the same FUSE-T `Connection closed` result.
  Source comparison against `macos-fuse-t/libfuse` identified the next concrete
  transport mismatch: FUSE-T's channel is a stream socket, and libfuse reads
  exactly one request frame by first reading `fuse_in_header` and then the
  remaining `header.len` bytes. fuser still used the Linux `/dev/fuse`
  assumption that a single large `read()` returns exactly one request packet,
  which can drop coalesced socket frames. The next fuser patch makes macOS
  receive one framed FUSE-T request per session-loop iteration while leaving the
  Linux read path unchanged.
- macOS live smoke through Operon's FUSE-T NFS adapter remains to be fixed and
  validated on GitHub-hosted macOS or on a suitable self-hosted FUSE-T runner.
- Public release publication and post-release README Quickstart validation remain
  outside this in-progress implementation checkpoint.
