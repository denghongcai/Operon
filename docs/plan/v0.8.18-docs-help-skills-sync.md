# v0.8.18 Docs, Help, and Skills Synchronization

Goal: keep user-facing docs, CLI help, repo-local skills, and agent rules in
lockstep with the implemented endpoint-only model.

## Scope

- Audit current docs, repo-local skills, and CLI help paths for stale command
  examples and provider-model drift.
- Add a validation script that checks documented command surfaces against the
  real clap help model.
- Add an AGENTS.md rule requiring future CLI, config, endpoint, or skill changes
  to update docs and run the synchronization validation.
- Keep mDNS framed as endpoint discovery convenience, not a provider model.

## Done When

- repo-local skills use current endpoint-only discovery commands.
- current docs do not instruct users to run removed provider commands or
  legacy discovery flags.
- [`scripts/verify-docs-help-skills-sync.sh`](../../scripts/verify-docs-help-skills-sync.sh) validates CLI help paths, skills,
  AGENTS.md synchronization rules, and stale provider command examples.
- CI runs the synchronization validation.

## Completed

- Added [`scripts/verify-docs-help-skills-sync.sh`](../../scripts/verify-docs-help-skills-sync.sh).
- Updated repo-local skills and planning docs to use current mDNS discovery
  syntax.
- Added graph/workflow help validation to the skills/help checks.
- Added AGENTS.md synchronization rules for future docs, help, and skills
  changes.
- Added the synchronization validation to CI and README validation guidance.

## Remaining

- No v0.8.18 work remains.
