# v0.16.6 Release Publication and Public Verification

Status: Completed.

## Goal

Publish the current v0.16 release line from `main` after the v0.18.3 SDK public
API contract hardening and v0.18.4 daemon startup/config error semantics work,
then verify public artifacts and README Quickstart against the published
release.

## Scope

1. Confirm the release commit is merged to `main` before tagging.
2. Align Rust crate versions, TypeScript SDK package version,
   `PROTOCOL_VERSION`, CLI `--version`, release dry-run inputs, and README
   Quickstart workflow examples to `0.16.6` / `v0.16.6`.
3. Run main CI and CodeQL on the release commit.
4. Run the manual cross-platform live mount release gates for macOS FUSE-T and
   Windows WinFsp.
5. Build draft release artifacts through the release workflow.
6. Publish the release and verify public artifacts plus README Quickstart
   through GitHub Actions.
7. Record run IDs, tag, commit, and remaining limits in this phase plan.

## Acceptance

- The release tag is created from the intended `main` commit.
- CI, CodeQL, live mount release gates, release artifact verification, and
  README Quickstart verification pass for the published release.
- README, PROTOCOL, AGENTS, and phase docs agree on the released version line.

## Progress

- Release publication phase added.
- Aligned local release-line versions and validation expectations to
  `0.16.6` / `v0.16.6`.
- Committed and pushed release commit
  `d38869a56cc2a4b247788d702e1e7cb6b9c74d74` to `main`.
- Main CI passed in workflow run `25507449800`.
- CodeQL passed in workflow run `25507448153`.
- Cross-platform live mount release gates passed in workflow run `25507467831`,
  including macOS FUSE-T hosted live mount and Windows WinFsp live mount.
- Release gate validation passed for tag `v0.16.6` on the release commit.
- Created and pushed public tag `v0.16.6` from the release commit.
- Draft Release workflow run `25507784221` built Linux, macOS, Windows, SDK,
  and `SHA256SUMS` artifacts and completed successfully.
- Published GitHub Release `v0.16.6` at
  `https://github.com/denghongcai/Operon/releases/tag/v0.16.6`.
- Public release artifact verification passed on Ubuntu, macOS, and Windows in
  workflow run `25508118147`.
- README Quickstart verification passed against the published release in
  workflow run `25508118139`.

## Remaining

- No v0.16.6 release publication or public verification work remains.
