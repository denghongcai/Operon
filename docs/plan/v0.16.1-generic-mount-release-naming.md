# v0.16.1 Generic Mount and Release Naming Cleanup

Status: Completed.

## Goal

Make live mount and release-gate maintenance entrypoints version-neutral while
preserving v0.14 compatibility wrappers for historical docs and automation.

## Scope

1. Add generic live-mount workflow and script names.
2. Keep v0.14 script names as compatibility wrappers.
3. Update release packaging to call generic FUSE-T install helpers.
4. Keep release-gate verification generic while accepting historical v0.14
   workflow evidence.
5. Add validation coverage for the generic names and compatibility wrappers.

## Completed

- Added `.github/workflows/live-mount-smoke.yml` as the active
  `Cross-Platform Live Mount Smoke` workflow.
- Added generic mount smoke helper names:
  `scripts/install-macos-fuse-t.sh`,
  `scripts/preflight-macos-fuse-t-host.sh`,
  `scripts/smoke-macos-live-mount.sh`,
  `scripts/smoke-macos-fuse-zip-probe.sh`,
  `scripts/smoke-macos-fuser-hello-probe.sh`,
  `scripts/smoke-macos-libfuse-lowlevel-hello-probe.sh`, and
  `scripts/smoke-windows-live-mount.ps1`.
- Kept the older `scripts/*v0.14*` names as compatibility wrappers.
- Updated release draft packaging to call `scripts/install-macos-fuse-t.sh`.
- Added `scripts/verify-v0.16.1-generic-mount-release-naming.sh` and wired it
  into the consolidated validation runner.

## Remaining

- No v0.16.1 generic mount/release naming cleanup work remains.
