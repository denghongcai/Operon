# v0.16.9 Architecture Boundary Release Publication

Status: Completed.

## Goal

Publish the post-v0.16.8 architecture boundary cleanup on the public v0.16
release line, then verify cross-platform artifacts and installation paths.

## Version-Line Note

The v0.18.12 phase name is an internal maintenance batch identifier. Public
artifacts continue the stable v0.16 patch release line as `v0.16.9`.

## Plan

1. Align Rust crates, the TypeScript SDK, protocol version, CLI assertions,
   release examples, workflows, and focused validation to `0.16.9` / `v0.16.9`.
2. Repair historical validation routing after protocol conversion extraction.
3. Run local release validation and push the release-preparation commit.
4. Require CI and CodeQL success on the exact release commit.
5. Run cross-platform live mount and Windows runner pre-tag gates.
6. Push `v0.16.9`, wait for Draft Release artifacts, and publish the release.
7. Run public artifact, install usability, and README Quickstart verification.
8. Record exact workflow evidence here and in the phase tracker.

## Progress

- Release publication phase added.
- Aligned Rust crates, the TypeScript SDK, protocol version, CLI assertions,
  release examples, and workflow defaults to `0.16.9` / `v0.16.9`.
- Repaired historical protocol validation routing after conversion extraction.
- Local release preparation passed Rust checks, SDK typecheck/tests/build,
  docs/help/skills synchronization, focused release validation, and all 48
  consolidated core validation scripts.
- Release commit `3b13a4ebb35a8552a2807f2f894b99883c50e9c0` was pushed to
  `main` and tagged as `v0.16.9`.
- CI `33240581793`, CodeQL `33240581596`, Cross-Platform Live Mount Smoke
  `33240803280`, and Windows Runner Image Smoke `33240802218` passed on the
  release commit.
- Draft Release `33241165888` built the Linux, macOS, Windows, TypeScript SDK,
  and checksum assets, and the release was published at
  `https://github.com/denghongcai/Operon/releases/tag/v0.16.9`.
- Release artifact verification `33241405981`, install usability
  `33241409216`, and README Quickstart `33241407123` passed against the public
  release.
- Both `pretag` and `postrelease` release gate orchestration checks passed for
  the exact release commit.

## Verification Evidence

- `cargo check --workspace --locked`
- `pnpm --filter @operon/sdk typecheck`
- `pnpm --filter @operon/sdk test`
- `pnpm --filter @operon/sdk build`
- `scripts/verify-docs-help-skills-sync.sh`
- `scripts/verify-v0.16.9-release-publication.sh`
- `scripts/ci/run-validations.sh core`

## Remaining

- No v0.16.9 release publication or public verification work remains.
