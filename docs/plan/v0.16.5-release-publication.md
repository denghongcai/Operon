# v0.16.5 Release Publication and Public Verification

Status: In progress.

## Goal

Publish the v0.16 release line from `main` after the completed v0.16.0-v0.16.4
mount/runtime hardening work, and verify the public artifacts and README
Quickstart against the published release.

## Scope

1. Confirm the release commit is merged to `main` before tagging.
2. Align Rust crate versions, TypeScript SDK package version,
   `PROTOCOL_VERSION`, CLI `--version`, README install examples, and release
   docs to the v0.16 release line.
3. Run main CI and CodeQL on the release commit.
4. Run the manual cross-platform live mount release gates for macOS FUSE-T and
   Windows WinFsp.
5. Build draft release artifacts through the release workflow.
6. Publish the release and verify public artifacts plus README Quickstart
   through GitHub Actions.
7. Record run IDs, tag, commit, and remaining limits in the phase plan.

## Acceptance

- The release tag is created from the intended `main` commit.
- CI, CodeQL, live mount release gates, release artifact verification, and
  README Quickstart verification pass for the published release.
- README, PROTOCOL, AGENTS, and phase docs agree on the released version line.

## Progress

- Aligned Rust crate versions, the TypeScript SDK package version,
  `PROTOCOL_VERSION`, CLI version tests, release artifact dry-run defaults, and
  README Quickstart workflow examples to `0.16.5` / `v0.16.5`.
- Verified the local release line with:
  `cargo test -p operon-protocol --locked protocol_version_matches_grpc_release_line`,
  `cargo run -q -p operon-cli -- --version`,
  `cargo run -q -p operond -- --version`,
  `scripts/verify-release-artifacts.sh --dry-run v0.16.5`, and
  `OPERON_VERSION=v0.16.5 scripts/verify-readme-quickstart-docker.sh --dry-run`.

## Remaining

- Full v0.16.5 release publication and public verification remain.
